# 生产环境配置 — 只覆盖与默认值不同的部分，其余继承 application.yml

kele:
  doc:
    docStorage:
      minio:
        endpoint: http://kele-doc-minio:9000
        bucket: kele-doc
        secretKey: Taeon@0727
    fileUpload:
      minio:
        endpoint: http://kele-doc-minio:9000
        bucket: kele-doc
        secretKey: Taeon@0727
    staticResources:
      - pathPatterns: /static/**
        resourceLocations: file:///${kele.doc.fileUpload.path}
      - pathPatterns: /**
        resourceLocations: classpath:/static/kele-doc/

spring:
  datasource:
    druid:
      url: jdbc:mysql://kele-doc-mysql:3306/kele-doc?useUnicode=true&characterEncoding=utf8&autoReconnect=true&allowMultiQueries=true
      password: Taeon@0727
  redis:
    host: kele-doc-redis
    password: Taeon@0727
